/**
 *	Write a program that takes a number as input and displays its square.
 * @param {number} num
 * @returns square of Number
 */
function displaySquare(num) {
  return (square = num ** 2);
}
module.exports = displaySquare;
