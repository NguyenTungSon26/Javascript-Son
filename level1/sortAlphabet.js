/**
 *	Write a program that takes a list of strings as input and sorts the list in alphabetical order.
 * @param {*
 * strings[]} strings
 * @returns sort Alphabet
 */
function sortAlphabet(strings) {
  strings.sort();
  return strings;
}
module.exports = sortAlphabet;
