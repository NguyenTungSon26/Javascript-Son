/**
 * 1. Reverses: Cho đầu vào là 1 mảng, Viết một function để đảo ngược thứ tự phần tử trong mảng
 * @param {numbers[]} arr
 * @returns đảo ngược thứ tự phần tử trong mảng
 */
function reverseArray(arr) {
  return arr.reverse();
}

module.exports = reverseArray;
